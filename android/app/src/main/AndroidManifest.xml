<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/Theme.TowerDefense"
        android:hardwareAccelerated="true">

        <!-- Spielebibliothek als Einstiegspunkt -->
        <activity
            android:name=".LauncherActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🏰 Tower Defense (Hoch- und Querformat) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- 💎 Kristallkrieg (Querformat) -->
        <activity
            android:name=".kristall.KristallActivity"
            android:screenOrientation="sensorLandscape"
            android:exported="false" />

        <!-- 🦠 Zellkrieg (Hoch- und Querformat; im Hochformat wird die Welt gedreht) -->
        <activity
            android:name=".zell.ZellActivity"
            android:exported="false" />
    </application>
</manifest>
